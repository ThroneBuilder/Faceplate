
# Gallery persistent storage directory.
# Production: mount a persistent disk at this path (e.g., Render.com Disk).
# Local dev: defaults to ./gallery-data/ (gitignored).
GALLERY_DATA_DIR=/data/gallery
