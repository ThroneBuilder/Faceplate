---
import MosaicMaskEditor from './MosaicMaskEditor.astro'
import { getPublicGalleries } from '../lib/gallery/config.js'
const galleries = getPublicGalleries()
---
<section id="face-shaping-section" hidden aria-label="Face shaping">

  <div>
    <h2 class="step-heading">Step 3 — Shape your face</h2>
    <p class="step-instruction">Click the edge of your face to trim the edge of the faceplate.</p>
  </div>

  <div class="face-shaping-layout">

    <!-- Left / above: action sidebar (mirrors acq-buttons) -->
    <div class="face-shaping-sidebar">

      <p class="sidebar-label">Export</p>

      <button id="download-btn" class="sidebar-btn sidebar-btn--download" type="button">
        Download
      </button>

      <div class="sidebar-gap"></div>

      <p class="sidebar-label">Share</p>

      <label class="field-label" for="share-name-input">Name</label>
      <input
        id="share-name-input"
        class="share-name-input"
        type="text"
        maxlength="80"
        required
        placeholder="Your name"
        aria-label="Your name for this submission"
      />
      <p id="share-name-error" class="group-error" hidden></p>

      <button id="add-to-group-btn" class="sidebar-btn sidebar-btn--group" type="button">
        Add to Gallery
      </button>
      <select id="group-name-input" class="group-name-input" aria-label="Gallery">
        {galleries.map(g => (
          <option value={g.slug} selected={g.slug === 'hall-of-faces'}>{g.displayName}</option>
        ))}
      </select>
      <p id="group-error" class="group-error" hidden></p>

    </div>

    <!-- Right / below: mask editor + cubby projection + plate preview -->
    <div class="face-shaping-panels">
      <div class="panel panel--editor">
        <MosaicMaskEditor />
      </div>
      <div class="panel panel--cubby">
        <canvas id="cubby-canvas" class="cubby-canvas"></canvas>
      </div>
      <div class="panel panel--plate-preview">
        <div class="plate-preview-stack">
          <div class="plate-preview-item">
            <span class="plate-label">Front</span>
            <canvas id="plate-top-canvas" class="plate-canvas"></canvas>
          </div>
          <div class="plate-preview-item">
            <span class="plate-label">Back</span>
            <canvas id="plate-bottom-canvas" class="plate-canvas"></canvas>
          </div>
        </div>
      </div>
    </div>

  </div>

</section>

<style>
  #face-shaping-section {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

/* ── Two-column layout: sidebar left, panels right (mirrors acq-layout) ── */
  .face-shaping-layout {
    display: flex;
    gap: 1.5rem;
    align-items: flex-start;
    flex-wrap: wrap;
  }

  .face-shaping-sidebar {
    display: flex;
    flex-direction: column;   /* stacked on wide screens */
    gap: 0.6rem;
    min-width: 180px;
  }

  .face-shaping-panels {
    flex: 1;
    min-width: 240px;
    display: flex;
    gap: 1.5rem;
    align-items: flex-start;
    flex-wrap: wrap;
  }

  /* ── Narrow screens: sidebar goes side-by-side above panels ── */
  @media (max-width: 560px) {
    .face-shaping-sidebar {
      flex-direction: row;
      flex-wrap: wrap;
      min-width: unset;
      width: 100%;
    }
    .face-shaping-sidebar .sidebar-label { width: 100%; }
    .face-shaping-panels { min-width: unset; }
  }

  /* ── Sidebar labels ── */
  .sidebar-label {
    margin: 0;
    font-size: 0.72rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: #888;
  }

  .sidebar-gap { height: 0.4rem; }

  /* ── Sidebar buttons ── */
  .sidebar-btn {
    display: block;
    width: 100%;
    padding: 0.7rem 1rem;
    border: none;
    border-radius: 8px;
    font-size: 0.95rem;
    font-weight: 600;
    cursor: pointer;
    text-align: center;
    color: white;
    transition: background 0.15s, opacity 0.15s;
  }

  .sidebar-btn--download { background: #2563eb; cursor: pointer; }
  .sidebar-btn--download:hover { background: #1d4ed8; }
  .sidebar-btn--group    { background: #2c6e49; cursor: pointer; }
  .sidebar-btn--group:hover { background: #1f5235; }

  .group-error {
    margin: 0;
    font-size: 0.8rem;
    color: #c0392b;
    background: #fdf0ee;
    border: 1px solid #f5c6c0;
    border-radius: 6px;
    padding: 0.4rem 0.6rem;
  }

  .sidebar-btn:not(:disabled) {
    opacity: 1;
    cursor: pointer;
  }

  /* ── Field label ── */
  .field-label {
    margin: 0;
    font-size: 0.72rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: #888;
    display: block;
  }

  /* ── Name input ── */
  .share-name-input {
    width: 100%;
    padding: 0.45rem 0.6rem;
    border: 1px solid #ccc;
    border-radius: 6px;
    font-size: 0.88rem;
    color: #222;
    box-sizing: border-box;
    background: white;
  }

  /* ── Group name input ── */
  .group-name-input {
    width: 100%;
    padding: 0.45rem 0.6rem;
    border: 1px solid #ccc;
    border-radius: 6px;
    font-size: 0.88rem;
    color: #222;
    cursor: pointer;
    box-sizing: border-box;
    background: white;
  }

  /* ── Panels ── */
  .panel {
    flex: 1 1 0;
    min-width: 200px;
  }

  .cubby-canvas {
    width: 100%;
    height: auto;
    display: block;
    border-radius: 6px;
    border: 1px solid #ddd;
  }

  .panel--plate-preview {
    flex: 0 0 auto;
    min-width: 120px;
  }

  .plate-preview-stack {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .plate-preview-item {
    display: flex;
    flex-direction: column;
    flex: 1 1 90px;
    min-width: 80px;
    gap: 0.2rem;
  }

  .plate-label {
    font-size: 0.65rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: #888;
  }

  .plate-canvas {
    width: 100%;
    height: auto;
    display: block;
    border: 1px solid #ccc;
    border-radius: 4px;
    background: #f5f5f5;
  }

  @media (max-width: 600px) {
    .face-shaping-panels { flex-direction: column; }
    .panel { min-width: unset; width: 100%; }
  }
</style>
